import { TheOneClientOptions } from './types';

export const API_URL_BASE = 'https://the-one-api.dev/v2';

export const DEFAULT_CLIENT_OPTIONS: TheOneClientOptions = {
    shouldThrowOnError: false,
};
